function setCookie(name, value, days) {
  const expires = new Date(Date.now() + days * 864e5).toUTCString();
  document.cookie = name + '=' + encodeURIComponent(value) + '; expires=' + expires + '; path=/';
}

function getCookie(name) {
  const cookies = document.cookie.split('; ').reduce((r, v) => {
    const parts = v.split('=');
    return parts[0] === name ? decodeURIComponent(parts[1]) : r;
  }, '');
  return cookies;
}

function closeBanner() {
  setCookie('bannerClosed', 'true', 180);  // Set cookie for 180 days
  document.getElementById('announcement-banner').remove();  // Remove the banner when closed
}

function showBanner() {
  const bannerHTML = `
    <center id="announcement-banner">
      <b>Do you have suggestions?</b>
      <span style="float: right;">
        <b style="cursor: pointer; padding-left: 10px;" onclick="closeBanner()">&#10006;</b>
      </span>
    </center>
  `;
  document.body.insertAdjacentHTML('afterbegin', bannerHTML);  // Insert the banner into the DOM
}

function checkBanner() {
  const bannerClosed = getCookie('bannerClosed');
  if (bannerClosed !== 'true') {
    showBanner();  // Only show the banner if it hasn't been closed
  }
}

document.addEventListener('DOMContentLoaded', function () {
  checkBanner();  // Check if the banner should be shown when the page loads
});