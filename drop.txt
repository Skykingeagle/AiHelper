const flattenData = (data: Tag[]) => {
  return data.map(item => ({
    id: item.id,
    created_by_user: item.metadata.created_by_user,
    created_on: item.metadata.created_on,
    // Add more fields as needed
  }));
};